define(['./core'], function ($$) {
   
   // Namespace support is actually in core.js
   // This file just allows mapping to a keyword using the define/require call
   
   return $$.Namespace;
   
});